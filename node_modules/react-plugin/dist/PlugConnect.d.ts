import { ReactNode } from 'react';
import { PlugComponentType } from './types';
declare type Props = {
  children?: ReactNode;
  component: PlugComponentType<any, any>;
  pluginName: string;
  slotProps: object;
};
export declare function PlugConnect({
  children,
  component,
  pluginName,
  slotProps,
}: Props): import('react').ReactElement<
  import('./types').PlugProps<any, any>,
  | string
  | ((props: any) => import('react').ReactElement<any, any> | null)
  | (new (props: any) => import('react').Component<any, any, any>)
>;
export {};
