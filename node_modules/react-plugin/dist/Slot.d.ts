import { ReactNode } from 'react';
declare type Props = {
  children?: ReactNode;
  name: string;
  slotProps?: object;
};
export declare function Slot({ children, name, slotProps }: Props): JSX.Element;
export {};
