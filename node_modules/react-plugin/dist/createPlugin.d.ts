import { PluginArgs, PluginCreateApi, PluginSpec } from 'ui-plugin';
import { PlugComponentType } from './types';
interface CreateApi<T extends PluginSpec> extends PluginCreateApi<T> {
  plug<SlotProps extends {}>(
    slotName: string,
    component: PlugComponentType<T, SlotProps>,
  ): void;
  namedPlug<SlotProps extends {}>(
    slotName: string,
    plugName: string,
    component: PlugComponentType<T, SlotProps>,
  ): void;
}
export declare function createPlugin<T extends PluginSpec>(
  args: PluginArgs<T>,
): CreateApi<T>;
export {};
